/** The stage colour behind everything. Its colour comes from --scene-bg, set by the scroll controller. */
export function ExperienceBackground() {
  return <div className="scene-bg" aria-hidden="true" />;
}
